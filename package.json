{
  "name": "@condenast/tftest",
  "version": "1.1.1",
  "description": "Terraform test framework",
  "main": "bin/tftest",
  "scripts": {
    "postinstall": "npm run download-dependencies",
    "test": "istanbul cover jasmine && istanbul check-coverage coverage/coverage.json",
    "test-coverage": "istanbul cover jasmine",
    "lint": "semistandard",
    "lint-fix": "semistandard --fix | snazzy",
    "download-dependencies": "rm -f ./node_modules/.bin/tfjson-*; download-github-release -s `./scripts/get_os.js` conde-nast-international tfjson ./node_modules/.bin/; chmod +x ./node_modules/.bin/tfjson-*",
    "docker-build": "docker build -t tftest ."
  },
  "bin": {
    "tftest": "bin/tftest"
  },
  "author": "",
  "license": "ISC",
  "husky": {
    "hooks": {
      "pre-commit": "npm run test && npm run lint"
    }
  },
  "semistandard": {
    "ignore": [
      "spec/**"
    ]
  },
  "dependencies": {
    "download-github-release": "^0.3.2",
    "jasmine": "^3.2.0",
    "jasmine-spec-reporter": "^4.2.1"
  },
  "devDependencies": {
    "husky": "^1.0.0-rc.13",
    "semistandard": "^12.0.1",
    "istanbul": "^0.4.5",
    "snazzy": "^7.1.1"
  }
}
